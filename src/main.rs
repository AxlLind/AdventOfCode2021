use std::time::Instant;
use std::collections::*;
use itertools::Itertools;

fn main() {
  let now = Instant::now();
  // this is a template, add solution here
  println!("Part one: {}", 0);
  println!("Part two: {}", 0);
  println!("Time: {}ms", now.elapsed().as_millis());
}
