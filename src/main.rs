use std::time::Instant;

fn main() {
  let now = Instant::now();
  // this is a template, add solution here
  println!("Part one: {}", 0);
  println!("Part two: {}", 0);
  println!("Time: {}ms", now.elapsed().as_millis());
}
